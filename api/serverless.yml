org: danielferguson
app: bullseye
service: api
frameworkVersion: "3"

plugins:
  - serverless-plugin-typescript
  - serverless-offline

provider:
  name: aws
  runtime: nodejs14.x

functions:
  getAllArchers:
    handler: src/api/archers.getAll
    events:
      - httpApi:
          path: /api/archers
          method: GET
  createArcher:
    handler: src/api/archers.create
    events:
      - httpApi:
          path: /api/archers
          method: POST
  getArcher:
    handler: src/api/archers.get
    events:
      - httpApi:
          path: /api/archers/{archerId}
          method: GET
  deleteArcher:
    handler: src/api/archers.delete
    events:
      - httpApi:
          path: /api/archers/{archerId}
          method: DELETE
  getArcherGame:
    handler: src/api/archers.getArcherGame
    events:
      - httpApi:
          path: /api/archers/{archerId}/games/{gameId}
          method: GET
  deleteArcherGame:
    handler: src/api/archers.deleteArcherGame
    events:
      - httpApi:
          path: /api/archers/{archerId}/games/{gameId}
          method: DELETE
